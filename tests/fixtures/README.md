# Media test fixture

mobile-film.mp4 is a synthetic, silent 32×32 solid-color H.264 clip (8 s, 24 fps).
It is used only by browser tests for deterministic playback/failure/pause assertions.
It is never copied to public assets. The separately tested public movies are NASA ISS footage.
